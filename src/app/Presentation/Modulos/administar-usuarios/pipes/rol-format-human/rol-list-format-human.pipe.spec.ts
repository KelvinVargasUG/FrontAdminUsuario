import { RolListFormatHumanPipe } from './rol-list-format-human.pipe';

describe('RolFormatHumanPipe', () => {
  it('create an instance', () => {
    const pipe = new RolListFormatHumanPipe();
    expect(pipe).toBeTruthy();
  });
});
