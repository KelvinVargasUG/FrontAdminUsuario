import { RolFormatHumanPipe } from './rol-format-human.pipe';

describe('RolFormatHumanPipe', () => {
  it('create an instance', () => {
    const pipe = new RolFormatHumanPipe();
    expect(pipe).toBeTruthy();
  });
});
